{
  "name": "client",
  "version": "1.0.0",
  "main": "src/client.ts",
  "scripts": {
    "test": "jest",
    "devstart": "vite",
    "build": "vite build",
    "serve": "npm run build && vite preview"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "socket.io-client": "^4.5.1"
  },
  "devDependencies": {
    "jest": "^28.1.1",
    "ts-jest": "^28.0.5",
    "vite": "^2.9.12"
  }
}
